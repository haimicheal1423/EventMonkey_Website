const domContainer = document.querySelector('#interests-container');
const root = ReactDOM.createRoot(domContainer);
root.render(e(LikeButton));

import React from 'react';
import { ReactDOM } from 'react';